{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_group_show">
    {# <p>{{ 'group.show.name'|trans([], 'BaseUserBundle') }}: {{ group.getName() }}</p> #}
    <p>{{ 'group.show.name'|trans }}: {{ group.getName() }}</p>
</div>